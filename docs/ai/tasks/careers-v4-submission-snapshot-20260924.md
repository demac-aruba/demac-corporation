# Careers V4: original submitted presentation (LANG-04)
## Authority and scope
Christian authorized continuation after 35d996bc and requested a result/pending summary
per block. Deep Review: additive persistent evidence on the EXISTING application.
No new collection, roles, access rules, deployment configuration, real data or email.
Use existing careersApplications, session transaction, immutable jobSnapshot and private files.

## Existing / modified / missing / externally gated
- Existing: localized shared form, manual reviewed editorial versions, canonical server
  validation, atomic submit/one mail job and fingerprint replay, protected Recruitment.
- Modified: shared frozen-label primitives, submission request metadata, server-derived
  evidence and read-only original-answer presentation in both admin consumers.
- This block: bounded originals, selected language/version, role labels/options and
  standard question labels, explicit legacy unknown-language behavior.
- Still missing: full global/privacy localization, additional document categories,
  independent localized emails, persistent owner staging, full visual acceptance.
- External launch decisions remain sender/scanner/policies/assets/vacancies/release.

## Acceptance / failures
Only validated, visible, allowlisted profile fields and role answers may be captured.
Keep exact original strings (including whitespace, accents and HTML as plain text) while
retaining canonical contact/selection fields for existing consumers. Reject oversized
originals; do not silently truncate. Server derives all labels from the current approved
vacancy and shared presentation version, never from a supplied snapshot.
Explicitly versioned requests freeze locale in the replay fingerprint. Legacy profile-only
requests remain compatible; do not guess historical language from names or nationality.
Stale vacancy or presentation rejects atomically. Retry and session receipt preserve the
first committed evidence, even after an editorial update. No inference of suitability.
Test allowed/rejected edits, raw values, hidden fields, malicious snapshot spoofing,
pre/post-commit failure, unchanged permissions, private files and historical read fallback.
