# Careers finalization — candidate mail, documents and localized policy

## Scope and authority
Continuation explicitly requested by Christian. Internal mailbox delivery is deferred until
he creates and verifies the mailbox; do not configure either spelling supplied in conversation.
Existing application/session/mail collections, access control and Functions remain authoritative.
No production configuration, permission, secret, DNS, real applicant or mail write is authorized.

## Recovered work
Unreferenced preparation tree 0abd157d was recovered as 0c317859 on the existing branch.
It contains candidate EN/ES HTML/plain-text templates, frozen message records, classified
supporting documents and reviewed privacy/global-copy editors. It was not assumed tested.
The baseline exposed a controlled-textarea accessible-name defect; c0e55a6f uses independent
labels and stable React IDs. Later actual browser artifacts identified an exact-label
regression for the generic camera action and a test session injector running in a sandboxed
email iframe. These are corrected without widening the iframe sandbox or hiding page errors.

## Additional fixes
- Subject value is separately addressable; historical `sent` remains explicitly SMTP acceptance,
  not inbox delivery. Synthetic admin session initialization is top-frame only.
- Document replacement keeps the accepted source through scan/storage failure. The canonical
  transaction adopts the new generation and schedules the old generation for cleanup together.
  Only one replacement per session can be active; final file-slot/size rules and original
  file-count limits remain. Temporary replacement occupancy may exceed the final total by
  one maximum-sized file; cleanup and existing session expiry bound that overlap.
- Anonymous or submitted-session replacement is rejected. Lost commit acknowledgements use
  the existing ownership check; no successful target is treated as an orphan.
- Publication preflight explains the server's ID-policy gate; drafts are still allowed.
- Preview preserves date/URL editor types. Hero/list excerpts are display-only; original
  employer text stays in the role and submitted snapshots.
- Mandatory certificate, private replacement storage and concurrent candidate-email claims
  receive real Firebase-emulator coverage; internal message jobs remain absent by instruction.

## Verification and residual boundary
Focused local suites: 168 PASS, no FAIL/SKIP at preparation. Scripts pass syntax checks.
Exact final-head build, browser integration and screenshot outcomes must be recorded in PR 527
from CI evidence before calling this increment verified. No weakening of existing assertions.
Review mode: Solo Maintainer Adversarial Review (same implementer in separate pass), not independent.
Persistent owner staging, approved autonomous hero artwork, real sender/scanner/policy activation,
physical devices and internal-mailbox delivery remain separate, unverified release requirements.
No alternative storage authority or browser-persisted candidate database is introduced.
Rollback: coherent revert of this increment; do not delete historical snapshots or stored files.
