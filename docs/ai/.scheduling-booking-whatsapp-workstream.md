# Scheduling / Booking / WhatsApp Consolidated Workstream

Status: Architecture investigation complete; implementation is proceeding package-by-package on this branch.

Tracking issue: #448
Draft PR: #449

No production merge or deployment is authorized by this workstream.

## Engineering constraints

- Booking Authority remains the only authority for appointment placement, capacity validation, capacity locks, and customer-facing appointment identity.
- Communication Authority / transactional WhatsApp services remain the outbound communication authority.
- Canonical workforce records remain the source for van crew identity: `staffProfiles`, dated `dailyVanAssignments`, plus canonical Van fallbacks.
- No browser-only or WhatsApp-only scheduling authority may be introduced.
- Existing business rules remain unchanged unless explicitly listed under Approved changes below.
- Preserve transaction-time revalidation, idempotency, conflict prevention, support/primary linkage, audit history, and single customer communication ownership.
- No patch-on-patch implementations or duplicate duration/capacity formulas.
- Self-review is required. There is no independent third-party reviewer gate for this repository/workstream.

## Protected existing rules

1. Flexible lunch overlap: lunch is not a hard booking blocker. A continuous job may span the lunch window. Lunch is not independently sellable as a normal customer slot.
2. Same-property Standard Service: primary Van capacity is 7 units under the existing canonical rule; overflow uses linked support assignment(s).
3. Support assignments never send duplicate customer confirmation/reminder messages. The primary appointment owns customer communication.
4. Support Van selection must honor real capacity and operational/routing policy, not merely any nominally free Van.
5. Half-day, company closure, Van unavailable, staff availability and commit-time capacity checks remain authoritative.
6. After-hours work is an explicit operational exception and must not become ordinary customer-bookable capacity.
7. No production merge/deploy, destructive migration, or production messaging is authorized by this branch.

## Architecture findings

### Canonical scheduling and time

- `functions/bookingCapacityAvailability.js` derives canonical occupied capacity and returns assignment `endTime` from occupied slots.
- `functions/bookingAuthorityWorkOrders.js` persists `scheduledSlots`, `appointmentEndTime`, `appointmentDurationMinutes`, and PRIMARY/SUPPORT role on each Work Order.
- `apps/erp-next/lib/live-scheduling.ts` projects schedule end using canonical reserved slots first, then explicit appointment end, then duration as a historical fallback.
- Technician WhatsApp previously reconstructed the visible end from duration before canonical reserved capacity. That drift is now corrected: canonical reserved slots own the technician-visible schedule span, including half-day slot maps.

### Technician daily schedule / PENDIENTE

- PENDIENTE is derived from the same canonical Booking Authority slot primitives, not from browser whitespace.
- Regular sellable starts remain `08:30`, `09:30`, `10:30`, `13:30`, `14:30`, `15:30`; half-day policy reuses the canonical half-day slot map.
- Lunch is never emitted as an independently sellable PENDIENTE slot.
- Temporary holds reserve capacity but are intentionally omitted from technician work messages.
- Van/staff unavailability removes PENDIENTE rather than advertising unusable capacity.

### Same-day technician changes

- The existing `workOrders/{workOrderId}` write trigger remains the single event boundary.
- After the 08:00 daily schedule dispatch, newly active same-day work and material placement changes queue deterministic internal Van-group alerts through the existing transactional WhatsApp authority.
- Field execution status changes do not masquerade as schedule changes.
- Internal Van alerts never inherit customer recipients.

### Customer communication

- Customer confirmation/reminder rendering remains centralized through appointment/work-order communication services.
- Support Work Orders remain without customer notification ownership.
- No customer communication authority was duplicated by the technician-alert changes.

### New Appointment validation / crew identity

- `LiveAppointmentCreateDrawer` continues using canonical `checkOfficeCreateAvailability` and `confirmOfficeAppointment`.
- Capacity validation now runs debounced/live when the complete target changes; the manual action is only a recheck. Final transaction-time validation remains authoritative on confirm.
- Stale asynchronous validation responses are invalidated by signature + request epoch before they may become confirmable.
- The drawer reuses `liveVanCrew(...)`, which resolves dated `dailyVanAssignments` first and Van defaults second, rather than inventing another workforce lookup.

### Support Van

- Planned support is already implemented in the canonical scheduling engine and remains unchanged.
- Standard Service policy is verified by automated tests: one primary Van through seven same-property units; quantity above seven uses one linked support Van for the remainder when physical capacity exists.
- Support start candidates are filtered by real capacity and half-day policy; the New Appointment drawer already exposes valid support alternatives returned by Booking Authority and keeps the selected Van primary/responsible.
- Work Orders already model `appointmentAssignmentRole: primary|support` and parent linkage.

### Temporary Hold

- Booking Authority now supports first-class `temporary_hold` creation and confirmation while reusing the same appointment, Work Order and capacity-lock identities.
- Holds own real capacity, project into Scheduling as held/orange capacity, and suppress customer confirmation/reminder ownership until confirmed.
- Hold confirmation verifies lock ownership before transition; reschedule keeps the hold held/silent while atomically moving its locks; cancel uses the canonical lifecycle and releases capacity.
- Automatic expiry is intentionally not implemented because no canonical duration/policy exists. Expiry policy remains `NEEDS_HUMAN`; manual confirm/reschedule/cancel is the current explicit behavior.

### Ad-hoc rescue support

- Existing PRIMARY/SUPPORT Work Order model should be reused.
- A rescue support action should link a new SUPPORT resource assignment/Work Order to an existing appointment and reserve the selected supporting Van/time atomically.
- Both the support Van and primary Van require internal same-day schedule-change notices; no duplicate customer communication.

### After-hours

- Current regular schedule lanes expose only canonical operating slots.
- After-hours must be a separate explicit action, starting at/after the normal 17:00 boundary, with no required scheduled end for emergency work.
- Actual completion time must come from field/work-order execution and feed existing employee timesheet/overtime fields rather than from a fabricated planned duration.
- Existing attendance/payroll already stores `overtimeMinutes`/`overtimeHours` and calculates time after 17:00; integration should reuse this authority.

## Approved changes / implementation checklist

- [x] 01 — Canonical schedule time synchronization: technician schedule + customer communication regression audit.
- [x] 02 — Technician daily WhatsApp: include sellable empty periods as `PENDIENTE`; do not mark lunch/off/closure/unavailable periods pending.
- [x] 03 — Same-day schedule-change notification pipeline for newly confirmed/materially changed same-day work.
- [x] 04 — New Appointment header: show actual dated Van driver/helper using existing crew resolver.
- [x] 05 — Replace manual-only Final Capacity Validation UX with debounced live validation backed by existing Booking Authority; keep authoritative commit validation.
- [x] 06 — First-class Temporary Hold lifecycle/capacity state + orange Scheduling representation + confirm transition; no customer messages while held. `NEEDS_HUMAN`: automatic expiry duration/policy is not defined.
- [x] 07 — Verify/complete planned support-Van UX using existing 7+remainder engine and exact valid support-time choices. No new engine was needed.
- [ ] 08 — Add ad-hoc `Enviar apoyo a compañero` from an open slot using same PRIMARY/SUPPORT architecture.
- [ ] 09 — Same-day support notifications to both support Van and primary Van, with actual dated crew identity.
- [ ] 10 — Add explicit After-Hours / Emergency scheduling action outside normal sellable capacity; open-ended planned duration.
- [ ] 11 — Connect after-hours actual completion to timesheet/overtime authority without creating a second payroll calculation source.
- [ ] 12 — Full cross-flow regression review: booking, reschedule, cancel, support, hold, same-day change, WhatsApp, half-day, lunch overlap, concurrency, idempotency.
- [ ] 13 — Four self-review passes: correctness; architecture; integration/regression; production readiness.
- [ ] 14 — Required tests/lint/build/quality gates and final PR readiness report.

## Validation evidence so far

- Package 01 regression exposed one stale-date test fixture; the fixture was moved to a future date without weakening the production rule that rejects reminders for past appointments.
- Temporary Hold creation/confirm/reschedule/lost-lock regression tests are now part of the aggregate `test:booking-authority` gate rather than existing only as standalone files.
- Commit `8f5012e715d9a2b4306fc49a1698cba0877bc5c9` passed Office Booking Authority, Transactional WhatsApp, WhatsApp Wacli, Customer Agent Architecture, Customer Agent Production, Workforce Admin Functions, Marketing Agent, ERP Next CI, TypeScript/Web Build, and the dedicated Van Schedule Architecture workflow.
- The dedicated Van Schedule Architecture workflow owns regression checks for canonical technician end times, PENDIENTE capacity, same-day schedule-change policy, and technician schedule rendering.

## Progress

- Architecture/protocol audit: COMPLETE.
- Packages complete: 7 / 14 (01–07).
- Packages remaining: 7 / 14.
- Temporary Hold expiry policy: `NEEDS_HUMAN`; engineering may continue on packages 08–11 without inventing it.
- Production merge/deploy: NOT AUTHORIZED / NOT PERFORMED.
