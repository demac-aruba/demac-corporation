--- a/src/features/technicianPortal/contracts.ts
+++ b/src/features/technicianPortal/contracts.ts
@@ -38,6 +38,9 @@
 
 // AI_PROFESSIONAL_REPORTS_V8: editable customer-facing narrative generated from verified field data.
 export type ProfessionalReportStatus = 'pending' | 'processing' | 'completed' | 'failed';
+// MULTILINGUAL_PROFESSIONAL_REPORTS_V9: approved Spanish plus on-demand customer translations.
+export type ProfessionalReportLanguage = 'es' | 'pap_aw' | 'en';
+export type ProfessionalReportTranslationStatus = 'pending' | 'processing' | 'completed' | 'failed';
 
 export interface ProfessionalCustomerReport {
   executiveSummary: string;
@@ -49,6 +52,22 @@
   customerConclusion: string;
 }
 
+export interface PapiamentoCorrection {
+  id: string;
+  language: 'pap_aw';
+  sectionKey: keyof ProfessionalCustomerReport;
+  sourceText: string;
+  generatedText: string;
+  correctedText: string;
+  active: boolean;
+  source: 'office_review';
+  interventionId: string;
+  createdAt: string;
+  createdByUserId: string;
+  createdByName: string;
+  updatedAt: string;
+}
+
 export type ReportSectionType =
   | 'identification'
   | 'indoor'
@@ -177,6 +196,19 @@
   professionalReportSourceKey?: string;
   professionalReportModel?: string;
   professionalReportError?: string;
+  professionalReportTranslations?: Partial<Record<'pap_aw' | 'en', ProfessionalCustomerReport>>;
+  professionalReportTranslationStatus?: Partial<Record<'pap_aw' | 'en', ProfessionalReportTranslationStatus>>;
+  professionalReportTranslationRequestedAt?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationStartedAt?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationGeneratedAt?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationFailedAt?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationEditedAt?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationEditedByName?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationSourceKey?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationModel?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationError?: Partial<Record<'pap_aw' | 'en', string>>;
+  professionalReportTranslationUnknownWords?: { pap_aw?: string[] };
+  professionalReportTranslationVocabulary?: { pap_aw?: { source: string; sourceUrl: string; orthographyVersion: string; wordCount: number } };
   resultCode?: string;
   resultNotes?: string;
   customerReportNote?: string;
--- a/src/screens/OfficeReportReviewScreen.tsx
+++ b/src/screens/OfficeReportReviewScreen.tsx
@@ -1,13 +1,15 @@
 import React, { useEffect, useMemo, useState } from 'react';
 // TECHNICIAN_WORKFLOW_V7: voice notes render their automatic transcript in office and PDF output.
+// MULTILINGUAL_PROFESSIONAL_REPORTS_V9: original/professional views, translated PDFs and approved language memory.
 import { Image, Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
 import { AppModal, Button, Card, EmptyState, Input, Pill, SectionTitle } from '../components/UI';
-import { InterventionStatus, InterventionType, ProfessionalCustomerReport, ReportSection, WorkIntervention } from '../features/technicianPortal/contracts';
+import { InterventionStatus, InterventionType, PapiamentoCorrection, ProfessionalCustomerReport, ProfessionalReportLanguage, ReportSection, WorkIntervention } from '../features/technicianPortal/contracts';
 import { getTechnicianReportTemplate, templateFieldIsVisible, TemplateFieldDefinition } from '../features/technicianPortal/templates';
 import { workVisitAddOnDefinition } from '../features/technicianPortal/addOns';
 import { unitHasCompletedReview, visitHasCompletedReview } from '../features/technicianPortal/closure';
 import { createReportPrintWindow, PrintableTechnicalReport, renderPrintableTechnicalReport } from '../services/reportPrint';
 import { createTechnicalReportPdfDownload } from '../services/technicalReportPdf';
+import { listFirestoreCollection, saveFirestoreDocument } from '../services/firebase';
 import { useAppState } from '../state/AppState';
 import { useTeamState } from '../state/TeamState';
 import { useTechnicianPortalState } from '../state/TechnicianPortalState';
@@ -20,6 +22,45 @@
 
 // AI_PROFESSIONAL_REPORTS_V8: the office owns the final customer wording while technical evidence remains immutable.
 type ProfessionalReportEditor = Omit<ProfessionalCustomerReport, 'recommendations'> & { recommendations: string };
+type TranslationLanguage = Exclude<ProfessionalReportLanguage, 'es'>;
+type ReportView = 'original' | 'professional';
+
+const REPORT_LANGUAGE_COPY = {
+  es: {
+    code: 'ES', name: 'Español', reportTitle: 'Reporte técnico', professionalTitle: 'Resumen profesional del servicio',
+    prepared: 'Preparado para el cliente', executiveSummary: 'Resumen ejecutivo', workPerformed: 'Trabajo realizado',
+    technicalFindings: 'Hallazgos técnicos', measurementsAssessment: 'Evaluación de mediciones', recommendations: 'Recomendaciones',
+    safetyObservations: 'Observaciones de seguridad', customerConclusion: 'Conclusión para el cliente', technicalAppendix: 'Anexo técnico y fotográfico',
+    completed: 'Completada', notApplicable: 'No aplica', incomplete: 'Incompleta', registered: 'Registrados',
+    labels: { client: 'Cliente', property: 'Propiedad', address: 'Dirección', order: 'Orden', equipment: 'Aire acondicionado', work: 'Trabajo', reportDate: 'Fecha del reporte', observation: 'Observaciones', footer: 'Reporte técnico generado por DEMAC Professional Cooling Solutions', page: 'Página' },
+  },
+  pap_aw: {
+    code: 'PAP-AW', name: 'Papiamento (Aruba)', reportTitle: 'Reporte tecnico', professionalTitle: 'Resumen profesional di e servicio',
+    prepared: 'Prepara pa e cliente', executiveSummary: 'Resumen ehecutivo', workPerformed: 'Trabou ehecuta',
+    technicalFindings: 'Hallazgo tecnico', measurementsAssessment: 'Evaluacion di medida', recommendations: 'Recomendacion',
+    safetyObservations: 'Observacion di riesgo', customerConclusion: 'Conclusion pa e cliente', technicalAppendix: 'Anexo tecnico y fotografico',
+    completed: 'Completa', notApplicable: 'No aplicabel', incomplete: 'Incompleto', registered: 'Registra',
+    labels: { client: 'Cliente', property: 'Propiedad', address: 'Direccion', order: 'Orden', equipment: 'Airco', work: 'Trabou', reportDate: 'Fecha di reporte', observation: 'Observacion', footer: 'Reporte tecnico genera door di DEMAC Professional Cooling Solutions', page: 'Pagina' },
+  },
+  en: {
+    code: 'EN', name: 'English', reportTitle: 'Technical report', professionalTitle: 'Professional service summary',
+    prepared: 'Prepared for the customer', executiveSummary: 'Executive summary', workPerformed: 'Work performed',
+    technicalFindings: 'Technical findings', measurementsAssessment: 'Measurements assessment', recommendations: 'Recommendations',
+    safetyObservations: 'Safety observations', customerConclusion: 'Customer conclusion', technicalAppendix: 'Technical and photo appendix',
+    completed: 'Completed', notApplicable: 'Not applicable', incomplete: 'Incomplete', registered: 'Recorded',
+    labels: { client: 'Customer', property: 'Property', address: 'Address', order: 'Work order', equipment: 'Air conditioner', work: 'Work', reportDate: 'Report date', observation: 'Observations', footer: 'Technical report generated by DEMAC Professional Cooling Solutions', page: 'Page' },
+  },
+} satisfies Record<ProfessionalReportLanguage, {
+  code: string; name: string; reportTitle: string; professionalTitle: string; prepared: string;
+  executiveSummary: string; workPerformed: string; technicalFindings: string; measurementsAssessment: string;
+  recommendations: string; safetyObservations: string; customerConclusion: string; technicalAppendix: string;
+  completed: string; notApplicable: string; incomplete: string; registered: string;
+  labels: { client: string; property: string; address: string; order: string; equipment: string; work: string; reportDate: string; observation: string; footer: string; page: string };
+}>;
+
+const PROFESSIONAL_REPORT_FIELDS: Array<keyof ProfessionalCustomerReport> = [
+  'executiveSummary', 'workPerformed', 'technicalFindings', 'measurementsAssessment', 'recommendations', 'safetyObservations', 'customerConclusion',
+];
 
 const EMPTY_PROFESSIONAL_REPORT: ProfessionalReportEditor = {
   executiveSummary: '',
@@ -49,6 +90,11 @@
   return normalized.executiveSummary && normalized.workPerformed && normalized.customerConclusion ? normalized : undefined;
 }
 
+function professionalFieldText(report: ProfessionalCustomerReport | undefined, field: keyof ProfessionalCustomerReport) {
+  const value = report?.[field];
+  return Array.isArray(value) ? value.join('\n') : String(value ?? '').trim();
+}
+
 const WORK_LABELS: Record<InterventionType, string> = {
   standard_service: 'Servicio estándar',
   deep_service: 'Servicio profundo',
@@ -58,6 +104,21 @@
   checkup: 'Chequeo',
 };
 
+const TRANSLATED_WORK_LABELS: Record<TranslationLanguage, Record<InterventionType, string>> = {
+  pap_aw: {
+    standard_service: 'Servicio standard', deep_service: 'Servicio profundo', repair: 'Reparacion',
+    installation: 'Instalacion', diagnostic: 'Diagnostico', checkup: 'Control tecnico',
+  },
+  en: {
+    standard_service: 'Standard service', deep_service: 'Deep service', repair: 'Repair',
+    installation: 'Installation', diagnostic: 'Diagnostic', checkup: 'Checkup',
+  },
+};
+
+function workLabel(type: InterventionType, language: ProfessionalReportLanguage) {
+  return language === 'es' ? WORK_LABELS[type] : TRANSLATED_WORK_LABELS[language][type];
+}
+
 function statusLabel(status: InterventionStatus) {
   const labels: Record<InterventionStatus, string> = {
     draft: 'Por iniciar',
@@ -137,6 +198,11 @@
   const [lightboxEvidence, setLightboxEvidence] = useState<WorkOrderEvidence | null>(null);
   const [message, setMessage] = useState('Selecciona un reporte pendiente para revisar sus secciones, mediciones y fotografías.');
   const [professionalDraft, setProfessionalDraft] = useState<ProfessionalReportEditor>({ ...EMPTY_PROFESSIONAL_REPORT });
+  const [reportView, setReportView] = useState<ReportView>('original');
+  const [professionalLanguage, setProfessionalLanguage] = useState<ProfessionalReportLanguage>('es');
+  const [translationDraft, setTranslationDraft] = useState<ProfessionalReportEditor>({ ...EMPTY_PROFESSIONAL_REPORT });
+  const [rememberPapiamentoCorrection, setRememberPapiamentoCorrection] = useState(false);
+  const [papiamentoCorrections, setPapiamentoCorrections] = useState<PapiamentoCorrection[]>([]);
 
   useEffect(() => {
     if (requestedInterventionId) setSelectedInterventionId(requestedInterventionId);
@@ -155,6 +221,12 @@
   });
 
   const allowed = currentUser && ['admin', 'office', 'supervisor'].includes(currentUser.role);
+  useEffect(() => {
+    if (!allowed || currentUser?.authProvider !== 'firebase') return;
+    void listFirestoreCollection<PapiamentoCorrection>('papiamentoCorrections')
+      .then((items) => setPapiamentoCorrections(items.sort((first, second) => second.createdAt.localeCompare(first.createdAt))))
+      .catch(() => setPapiamentoCorrections([]));
+  }, [currentUser?.id, currentUser?.authProvider]);
   const reviewable = useMemo(() => workInterventions
     .filter((item) => item.status === 'ready_for_review' || item.status === 'changes_requested' || item.status === 'completed')
     .sort((first, second) => second.updatedAt.localeCompare(first.updatedAt)), [workInterventions]);
@@ -185,6 +257,12 @@
     setProfessionalDraft(professionalReportToEditor(selected?.professionalReport));
   }, [selected?.id, selected?.professionalReportGeneratedAt, selected?.professionalReportEditedAt]);
 
+  useEffect(() => {
+    const translated = professionalLanguage === 'es' ? undefined : selected?.professionalReportTranslations?.[professionalLanguage];
+    setTranslationDraft(professionalReportToEditor(translated));
+    setRememberPapiamentoCorrection(false);
+  }, [selected?.id, professionalLanguage, selected?.professionalReportTranslationGeneratedAt, selected?.professionalReportTranslationEditedAt]);
+
   const reportEvidenceIds = useMemo(
     () => unique([
       ...selectedSections.flatMap(({ section }) => section?.evidenceIds ?? []),
@@ -222,12 +300,17 @@
   function openReport(intervention: WorkIntervention) {
     setSelectedInterventionId(intervention.id);
     setCorrectionNote('');
+    setReportView('original');
+    setProfessionalLanguage('es');
+    setRememberPapiamentoCorrection(false);
     setMessage(`Revisando ${WORK_LABELS[intervention.type]}.`);
   }
 
   function closeReport() {
     setSelectedInterventionId('');
     setCorrectionNote('');
+    setReportView('original');
+    setProfessionalLanguage('es');
     setMessage('Selecciona otro reporte para revisar.');
   }
 
@@ -283,43 +366,161 @@
     return result.ok;
   }
 
+  function setTranslationField<K extends keyof ProfessionalReportEditor>(field: K, value: ProfessionalReportEditor[K]) {
+    setTranslationDraft((previous) => ({ ...previous, [field]: value }));
+  }
+
+  function reportForLanguage(language: ProfessionalReportLanguage) {
+    if (language === 'es') return professionalReportForOutput();
+    if (language === professionalLanguage) return editorToProfessionalReport(translationDraft) ?? selected?.professionalReportTranslations?.[language];
+    return selected?.professionalReportTranslations?.[language];
+  }
+
+  async function requestProfessionalTranslation(language: TranslationLanguage) {
+    if (!selected || !currentUser || selected.status !== 'completed' || !selected.professionalReport) {
+      setMessage('Primero finaliza y aprueba la versión profesional en español. Después podrás solicitar las traducciones.');
+      return;
+    }
+    setWorking(true);
+    setProfessionalLanguage(language);
+    const now = new Date().toISOString();
+    const result = await saveWorkIntervention({
+      ...selected,
+      professionalReportTranslationStatus: { ...(selected.professionalReportTranslationStatus ?? {}), [language]: 'pending' },
+      professionalReportTranslationRequestedAt: { ...(selected.professionalReportTranslationRequestedAt ?? {}), [language]: now },
+      professionalReportTranslationError: { ...(selected.professionalReportTranslationError ?? {}), [language]: '' },
+      updatedAt: now,
+      updatedByUserId: currentUser.id,
+      updatedByStaffId: (currentUser as { staffId?: string }).staffId ?? selected.updatedByStaffId,
+      updatedByName: currentUser.name,
+      version: Math.max(1, Number(selected.version ?? 1)) + 1,
+    });
+    setWorking(false);
+    setMessage(result.ok
+      ? `${REPORT_LANGUAGE_COPY[language].name}: traducción solicitada. Puedes salir; Firebase continuará en segundo plano.`
+      : result.message ?? 'No se pudo solicitar la traducción.');
+  }
+
+  async function saveProfessionalTranslation() {
+    if (!selected || !currentUser || professionalLanguage === 'es' || selected.status !== 'completed') return;
+    const translatedReport = editorToProfessionalReport(translationDraft);
+    if (!translatedReport) {
+      setMessage('Completa como mínimo el resumen, el trabajo realizado y la conclusión antes de guardar la traducción.');
+      return;
+    }
+
+    setWorking(true);
+    const language = professionalLanguage;
+    const now = new Date().toISOString();
+    const result = await saveWorkIntervention({
+      ...selected,
+      professionalReportTranslations: { ...(selected.professionalReportTranslations ?? {}), [language]: translatedReport },
+      professionalReportTranslationStatus: { ...(selected.professionalReportTranslationStatus ?? {}), [language]: 'completed' },
+      professionalReportTranslationEditedAt: { ...(selected.professionalReportTranslationEditedAt ?? {}), [language]: now },
+      professionalReportTranslationEditedByName: { ...(selected.professionalReportTranslationEditedByName ?? {}), [language]: currentUser.name },
+      updatedAt: now,
+      updatedByUserId: currentUser.id,
+      updatedByStaffId: (currentUser as { staffId?: string }).staffId ?? selected.updatedByStaffId,
+      updatedByName: currentUser.name,
+      version: Math.max(1, Number(selected.version ?? 1)) + 1,
+    });
+
+    if (!result.ok) {
+      setWorking(false);
+      setMessage(result.message ?? 'No se pudo guardar la traducción.');
+      return;
+    }
+
+    if (language === 'pap_aw' && rememberPapiamentoCorrection && currentUser.authProvider === 'firebase') {
+      const originalTranslation = selected.professionalReportTranslations?.pap_aw;
+      const changedFields = PROFESSIONAL_REPORT_FIELDS.filter((field) => professionalFieldText(originalTranslation, field) !== professionalFieldText(translatedReport, field));
+      try {
+        const created = await Promise.all(changedFields.map(async (field, index) => {
+          const correction: PapiamentoCorrection = {
+            id: `pap-aw-${selected.id}-${String(field)}-${Date.now()}-${index}`,
+            language: 'pap_aw',
+            sectionKey: field,
+            sourceText: professionalFieldText(selected.professionalReport, field),
+            generatedText: professionalFieldText(originalTranslation, field),
+            correctedText: professionalFieldText(translatedReport, field),
+            active: true,
+            source: 'office_review',
+            interventionId: selected.id,
+            createdAt: now,
+            createdByUserId: currentUser.id,
+            createdByName: currentUser.name,
+            updatedAt: now,
+          };
+          await saveFirestoreDocument('papiamentoCorrections', correction);
+          return correction;
+        }));
+        setPapiamentoCorrections((previous) => [...created, ...previous]);
+        setMessage(changedFields.length
+          ? `Traducción guardada y ${changedFields.length} corrección(es) añadida(s) a la memoria de Papiamento de Aruba.`
+          : 'Traducción guardada. No había cambios nuevos que añadir a la memoria.');
+      } catch (error) {
+        setMessage(`La traducción quedó guardada, pero la corrección no entró a la memoria: ${error instanceof Error ? error.message : String(error)}`);
+      }
+    } else {
+      setMessage('Traducción guardada para este reporte. No se modificó la memoria de idioma.');
+    }
+    setRememberPapiamentoCorrection(false);
+    setWorking(false);
+  }
+
+  async function togglePapiamentoCorrection(correction: PapiamentoCorrection) {
+    if (!currentUser || currentUser.authProvider !== 'firebase') return;
+    const updated = { ...correction, active: !correction.active, updatedAt: new Date().toISOString() };
+    try {
+      await saveFirestoreDocument('papiamentoCorrections', updated);
+      setPapiamentoCorrections((previous) => previous.map((item) => item.id === updated.id ? updated : item));
+      setMessage(updated.active ? 'Corrección activada para futuros reportes.' : 'Corrección desactivada; dejará de usarse en traducciones futuras.');
+    } catch (error) {
+      setMessage(`No se pudo actualizar la corrección: ${error instanceof Error ? error.message : String(error)}`);
+    }
+  }
+
   function professionalReportForOutput() {
     return editorToProfessionalReport(professionalDraft) ?? selected?.professionalReport;
   }
 
-  function buildPrintableReport(overrides: CustomerReportOverrides = {}): PrintableTechnicalReport | undefined {
+  function buildPrintableReport(overrides: CustomerReportOverrides = {}, language: ProfessionalReportLanguage = 'es'): PrintableTechnicalReport | undefined {
     if (!selected || !selectedUnit || !selectedTemplate) return undefined;
     const mainComponent = selectedEquipment?.components.find((item) => item.componentType === 'indoor') ?? selectedEquipment?.components[0];
-    const professionalReport = professionalReportForOutput();
+    const professionalReport = reportForLanguage(language);
+    const copy = REPORT_LANGUAGE_COPY[language];
+    if (!professionalReport) return undefined;
     return {
-      reportTitle: `Reporte técnico - ${WORK_LABELS[selected.type]}`,
-      reportCode: `REP-${selectedOrder?.id ?? selectedVisit?.workOrderId ?? selected.id}`,
+      reportTitle: `${copy.reportTitle} - ${workLabel(selected.type, language)}`,
+      reportCode: `REP-${selectedOrder?.id ?? selectedVisit?.workOrderId ?? selected.id}-${copy.code}`,
       clientName: selectedClient?.name ?? 'Cliente pendiente',
       propertyName: selectedProperty?.name ?? 'Propiedad principal',
       address: selectedProperty?.address ?? selectedOrder?.address ?? 'Dirección pendiente',
-      workType: WORK_LABELS[selected.type],
+      workType: workLabel(selected.type, language),
       equipmentName: selectedEquipment?.locationLabel ?? selectedUnit.locationLabel,
       equipmentDetails: `${mainComponent?.brand ?? 'Marca pendiente'} · ${mainComponent?.btu ? `${mainComponent.btu.toLocaleString('en-US')} BTU` : 'BTU pendiente'} · ${selectedEquipment?.systemType ?? 'Sistema pendiente'}`,
       orderId: selectedOrder?.id ?? selectedVisit?.workOrderId ?? 'Orden pendiente',
       reportDate: overrides.reportDate ?? formatDate(selected.reviewedAt ?? selected.updatedAt),
       observation: overrides.observation ?? selected.customerReportNote,
+      labels: copy.labels,
       sections: [
-        ...(professionalReport ? [{
-          title: 'Resumen profesional del servicio',
-          status: 'Preparado para el cliente',
+        {
+          title: copy.professionalTitle,
+          status: copy.prepared,
           fields: [
-            { label: 'Resumen ejecutivo', value: professionalReport.executiveSummary },
-            { label: 'Trabajo realizado', value: professionalReport.workPerformed },
-            { label: 'Hallazgos técnicos', value: professionalReport.technicalFindings },
-            { label: 'Evaluación de mediciones', value: professionalReport.measurementsAssessment },
-            { label: 'Recomendaciones', value: professionalReport.recommendations.length ? professionalReport.recommendations.map((item) => `• ${item}`).join('\n') : 'No se documentaron recomendaciones adicionales.' },
-            { label: 'Observaciones de seguridad', value: professionalReport.safetyObservations },
-            { label: 'Conclusión para el cliente', value: professionalReport.customerConclusion },
-          ],
-        }] : []),
-        ...selectedSections.map(({ definition, section }) => ({
-          title: definition.title,
-          status: section?.status === 'completed' ? 'Completada' : section?.status === 'not_applicable' ? 'No aplica' : 'Incompleta',
+            { label: copy.executiveSummary, value: professionalReport.executiveSummary },
+            { label: copy.workPerformed, value: professionalReport.workPerformed },
+            { label: copy.technicalFindings, value: professionalReport.technicalFindings },
+            { label: copy.measurementsAssessment, value: professionalReport.measurementsAssessment },
+            { label: copy.recommendations, value: professionalReport.recommendations.length ? professionalReport.recommendations.map((item) => `• ${item}`).join('\n') : '' },
+            { label: copy.safetyObservations, value: professionalReport.safetyObservations },
+            { label: copy.customerConclusion, value: professionalReport.customerConclusion },
+          ].filter((field) => Boolean(field.value?.trim())),
+        },
+        ...selectedSections.map(({ definition, section }, sectionIndex) => ({
+          title: sectionIndex === 0 ? `${copy.technicalAppendix}: ${definition.title}` : definition.title,
+          status: section?.status === 'completed' ? copy.completed : section?.status === 'not_applicable' ? copy.notApplicable : copy.incomplete,
+          pageBreakBefore: sectionIndex === 0,
           fields: definition.fields.filter((field) => templateFieldIsVisible(field, section?.fields ?? {})).map((field) => {
             const value = section?.fields[field.key];
             const evidence = evidenceFor(value);
@@ -337,7 +538,7 @@
         })),
         ...(selectedAddOns.length ? [{
           title: 'Add-ons instalados en esta visita',
-          status: 'Registrados',
+          status: copy.registered,
           fields: selectedAddOns.flatMap((addOn) => {
             const definition = workVisitAddOnDefinition(addOn.type);
             const before = evidenceFor(addOn.beforeEvidenceId);
@@ -353,8 +554,8 @@
     };
   }
 
-  async function openCustomerReport(overrides: CustomerReportOverrides = {}, targetWindow?: Window | null) {
-    const printable = buildPrintableReport(overrides);
+  async function openCustomerReport(language: ProfessionalReportLanguage = 'es', overrides: CustomerReportOverrides = {}, targetWindow?: Window | null) {
+    const printable = buildPrintableReport(overrides, language);
     if (!printable) return false;
     const popup = targetWindow ?? createReportPrintWindow();
     if (!popup) {
@@ -368,7 +569,7 @@
       return opened;
     } catch (error) {
       popup.close();
-      setMessage(`No se pudo preparar el PDF: ${error instanceof Error ? error.message : String(error)}`);
+      setMessage(`No se pudo preparar el PDF en ${REPORT_LANGUAGE_COPY[language].name}: ${error instanceof Error ? error.message : String(error)}`);
       return false;
     }
   }
@@ -455,7 +656,7 @@
       }
     }
 
-    const opened = await openCustomerReport({ reportDate: formatDate(now), observation: customerObservation || undefined }, reportWindow);
+    const opened = await openCustomerReport('es', { reportDate: formatDate(now), observation: customerObservation || undefined }, reportWindow);
     setWorking(false);
     setCorrectionNote('');
     reviewHistory.replace(JSON.stringify({ filter: 'approved', selectedInterventionId: selected.id }));
@@ -544,6 +745,14 @@
 
   if (selected && selectedTemplate && selectedUnit) {
     const mainComponent = selectedEquipment?.components.find((item) => item.componentType === 'indoor') ?? selectedEquipment?.components[0];
+    const activeDraft = professionalLanguage === 'es' ? professionalDraft : translationDraft;
+    const activeReport = editorToProfessionalReport(activeDraft);
+    const activeStatus = professionalLanguage === 'es'
+      ? selected.professionalReportStatus
+      : selected.professionalReportTranslationStatus?.[professionalLanguage];
+    const activeError = professionalLanguage === 'es'
+      ? selected.professionalReportError
+      : selected.professionalReportTranslationError?.[professionalLanguage];
     return (
       <ScrollView contentContainerStyle={styles.page} keyboardShouldPersistTaps="handled">
         <View style={styles.hero}>
@@ -566,37 +775,95 @@
 
         {selected.status === 'changes_requested' && selected.resultNotes ? <View style={styles.warningBox}><Text style={styles.warningTitle}>Corrección pendiente</Text><Text style={styles.warningText}>{selected.resultNotes}</Text></View> : null}
 
-        <Card>
-          <SectionTitle
-            title="Borrador profesional para el cliente"
-            subtitle="La IA organiza la transcripción, mediciones, hallazgos y trabajos registrados. La oficina puede revisar y editar cada texto."
-            action={<Pill
-              label={selected.professionalReportStatus === 'completed' ? 'Listo' : selected.professionalReportStatus === 'failed' ? 'Requiere atención' : selected.professionalReportStatus === 'pending' || selected.professionalReportStatus === 'processing' ? 'Generando' : 'No generado'}
-              tone={selected.professionalReportStatus === 'completed' ? 'success' : selected.professionalReportStatus === 'failed' ? 'warning' : selected.professionalReportStatus === 'pending' || selected.professionalReportStatus === 'processing' ? 'info' : 'neutral'}
-            />}
-          />
-          {selected.professionalReportStatus === 'pending' || selected.professionalReportStatus === 'processing' ? (
-            <View style={styles.aiProcessingBox}><Text style={styles.aiProcessingTitle}>Generación en proceso</Text><Text style={styles.aiProcessingText}>Puedes salir de esta pantalla. Firebase continuará preparando el borrador y la bandeja se actualizará automáticamente.</Text></View>
-          ) : null}
-          {selected.professionalReportStatus === 'failed' ? (
-            <View style={styles.warningBox}><Text style={styles.warningTitle}>No se pudo completar el borrador</Text><Text style={styles.warningText}>{selected.professionalReportError || 'El reporte técnico permanece intacto. Puedes solicitar la generación nuevamente.'}</Text></View>
-          ) : null}
-          {selected.professionalReportStatus !== 'pending' && selected.professionalReportStatus !== 'processing' && !editorToProfessionalReport(professionalDraft) && selected.status === 'ready_for_review' ? (
-            <Button variant="success" label={working ? 'Solicitando…' : 'Generar borrador profesional con IA'} disabled={working} onPress={() => void requestProfessionalReport()} />
-          ) : null}
-          {editorToProfessionalReport(professionalDraft) ? (
-            <View style={styles.aiDraftFields}>
-              <Input label="Resumen ejecutivo" value={professionalDraft.executiveSummary} onChangeText={(value) => setProfessionalField('executiveSummary', value)} multiline editable={!working && selected.status === 'ready_for_review'} />
-              <Input label="Trabajo realizado" value={professionalDraft.workPerformed} onChangeText={(value) => setProfessionalField('workPerformed', value)} multiline editable={!working && selected.status === 'ready_for_review'} />
-              <Input label="Hallazgos técnicos" value={professionalDraft.technicalFindings} onChangeText={(value) => setProfessionalField('technicalFindings', value)} multiline editable={!working && selected.status === 'ready_for_review'} />
-              <Input label="Evaluación de mediciones" value={professionalDraft.measurementsAssessment} onChangeText={(value) => setProfessionalField('measurementsAssessment', value)} multiline editable={!working && selected.status === 'ready_for_review'} />
-              <Input label="Recomendaciones — una por línea" value={professionalDraft.recommendations} onChangeText={(value) => setProfessionalField('recommendations', value)} multiline editable={!working && selected.status === 'ready_for_review'} />
-              <Input label="Observaciones de seguridad" value={professionalDraft.safetyObservations} onChangeText={(value) => setProfessionalField('safetyObservations', value)} multiline editable={!working && selected.status === 'ready_for_review'} />
-              <Input label="Conclusión para el cliente" value={professionalDraft.customerConclusion} onChangeText={(value) => setProfessionalField('customerConclusion', value)} multiline editable={!working && selected.status === 'ready_for_review'} />
-              {selected.status === 'ready_for_review' ? <View style={styles.actionRow}><Button variant="secondary" label={working ? 'Procesando…' : 'Regenerar con IA'} disabled={working} onPress={() => void requestProfessionalReport()} /><Button variant="success" label={working ? 'Guardando…' : 'Guardar borrador'} disabled={working} onPress={() => void saveProfessionalDraft()} /></View> : null}
+        <View style={styles.reportViewTabs}>
+          <Pressable onPress={() => setReportView('original')} style={[styles.reportViewTab, reportView === 'original' && styles.reportViewTabActive]}><Text style={[styles.reportViewTabText, reportView === 'original' && styles.reportViewTabTextActive]}>Reporte original</Text><Text style={styles.reportViewTabMeta}>Datos, fotos, audio y transcripción</Text></Pressable>
+          <Pressable onPress={() => setReportView('professional')} style={[styles.reportViewTab, reportView === 'professional' && styles.reportViewTabActive]}><Text style={[styles.reportViewTabText, reportView === 'professional' && styles.reportViewTabTextActive]}>Reporte profesional</Text><Text style={styles.reportViewTabMeta}>Versión breve para el cliente</Text></Pressable>
+        </View>
+
+        {reportView === 'professional' ? (
+          <Card>
+            <SectionTitle
+              title="Reporte profesional para el cliente"
+              subtitle="La versión en español se aprueba primero. Papiamento de Aruba e inglés se generan después, únicamente cuando la oficina los solicita."
+              action={<Pill
+                label={activeStatus === 'completed' ? 'Listo' : activeStatus === 'failed' ? 'Requiere atención' : activeStatus === 'pending' || activeStatus === 'processing' ? 'Generando' : 'No generado'}
+                tone={activeStatus === 'completed' ? 'success' : activeStatus === 'failed' ? 'warning' : activeStatus === 'pending' || activeStatus === 'processing' ? 'info' : 'neutral'}
+              />}
+            />
+            <View style={styles.languageRow}>
+              {(Object.keys(REPORT_LANGUAGE_COPY) as ProfessionalReportLanguage[]).map((language) => (
+                <Pressable key={language} onPress={() => setProfessionalLanguage(language)} style={[styles.languageChip, professionalLanguage === language && styles.languageChipActive]}>
+                  <Text style={[styles.languageChipText, professionalLanguage === language && styles.languageChipTextActive]}>{REPORT_LANGUAGE_COPY[language].name}</Text>
+                </Pressable>
+              ))}
             </View>
-          ) : null}
-        </Card>
+
+            {activeStatus === 'pending' || activeStatus === 'processing' ? (
+              <View style={styles.aiProcessingBox}><Text style={styles.aiProcessingTitle}>Generación en proceso</Text><Text style={styles.aiProcessingText}>No necesitas quedarte aquí. Firebase continuará en segundo plano y la bandeja se actualizará automáticamente.</Text></View>
+            ) : null}
+            {activeStatus === 'failed' ? (
+              <View style={styles.warningBox}><Text style={styles.warningTitle}>No se pudo completar esta versión</Text><Text style={styles.warningText}>{activeError || 'El reporte original permanece intacto. Puedes solicitar la generación nuevamente.'}</Text></View>
+            ) : null}
+
+            {professionalLanguage === 'es' && activeStatus !== 'pending' && activeStatus !== 'processing' && !activeReport && selected.status === 'ready_for_review' ? (
+              <Button variant="success" label={working ? 'Solicitando…' : 'Generar borrador profesional con IA'} disabled={working} onPress={() => void requestProfessionalReport()} />
+            ) : null}
+            {professionalLanguage !== 'es' && selected.status !== 'completed' ? (
+              <View style={styles.aiProcessingBox}><Text style={styles.aiProcessingTitle}>Traducción disponible después de aprobar</Text><Text style={styles.aiProcessingText}>Finaliza primero el reporte profesional en español. Así evitamos traducir un borrador que todavía puede cambiar.</Text></View>
+            ) : null}
+            {professionalLanguage !== 'es' && selected.status === 'completed' && activeStatus !== 'pending' && activeStatus !== 'processing' && !activeReport ? (
+              <Button variant="success" label={working ? 'Solicitando…' : `Generar ${REPORT_LANGUAGE_COPY[professionalLanguage].name}`} disabled={working} onPress={() => void requestProfessionalTranslation(professionalLanguage)} />
+            ) : null}
+
+            {activeReport ? (
+              <View style={styles.aiDraftFields}>
+                <Input label={REPORT_LANGUAGE_COPY[professionalLanguage].executiveSummary} value={activeDraft.executiveSummary} onChangeText={(value) => professionalLanguage === 'es' ? setProfessionalField('executiveSummary', value) : setTranslationField('executiveSummary', value)} multiline editable={!working && (professionalLanguage === 'es' ? selected.status === 'ready_for_review' : selected.status === 'completed')} />
+                <Input label={REPORT_LANGUAGE_COPY[professionalLanguage].workPerformed} value={activeDraft.workPerformed} onChangeText={(value) => professionalLanguage === 'es' ? setProfessionalField('workPerformed', value) : setTranslationField('workPerformed', value)} multiline editable={!working && (professionalLanguage === 'es' ? selected.status === 'ready_for_review' : selected.status === 'completed')} />
+                <Input label={REPORT_LANGUAGE_COPY[professionalLanguage].technicalFindings} value={activeDraft.technicalFindings} onChangeText={(value) => professionalLanguage === 'es' ? setProfessionalField('technicalFindings', value) : setTranslationField('technicalFindings', value)} multiline editable={!working && (professionalLanguage === 'es' ? selected.status === 'ready_for_review' : selected.status === 'completed')} />
+                <Input label={REPORT_LANGUAGE_COPY[professionalLanguage].measurementsAssessment} value={activeDraft.measurementsAssessment} onChangeText={(value) => professionalLanguage === 'es' ? setProfessionalField('measurementsAssessment', value) : setTranslationField('measurementsAssessment', value)} multiline editable={!working && (professionalLanguage === 'es' ? selected.status === 'ready_for_review' : selected.status === 'completed')} />
+                <Input label={`${REPORT_LANGUAGE_COPY[professionalLanguage].recommendations} — una por línea`} value={activeDraft.recommendations} onChangeText={(value) => professionalLanguage === 'es' ? setProfessionalField('recommendations', value) : setTranslationField('recommendations', value)} multiline editable={!working && (professionalLanguage === 'es' ? selected.status === 'ready_for_review' : selected.status === 'completed')} />
+                <Input label={REPORT_LANGUAGE_COPY[professionalLanguage].safetyObservations} value={activeDraft.safetyObservations} onChangeText={(value) => professionalLanguage === 'es' ? setProfessionalField('safetyObservations', value) : setTranslationField('safetyObservations', value)} multiline editable={!working && (professionalLanguage === 'es' ? selected.status === 'ready_for_review' : selected.status === 'completed')} />
+                <Input label={REPORT_LANGUAGE_COPY[professionalLanguage].customerConclusion} value={activeDraft.customerConclusion} onChangeText={(value) => professionalLanguage === 'es' ? setProfessionalField('customerConclusion', value) : setTranslationField('customerConclusion', value)} multiline editable={!working && (professionalLanguage === 'es' ? selected.status === 'ready_for_review' : selected.status === 'completed')} />
+
+                {professionalLanguage === 'pap_aw' && selected.professionalReportTranslationUnknownWords?.pap_aw?.length ? (
+                  <View style={styles.warningBox}>
+                    <Text style={styles.warningTitle}>Revisión ortográfica recomendada</Text>
+                    <Text style={styles.warningText}>El vocabulario oficial de Aruba no encontró: {selected.professionalReportTranslationUnknownWords.pap_aw.join(', ')}. Pueden ser marcas o términos HVAC correctos; la oficina decide.</Text>
+                    <Button compact variant="ghost" label="Consultar papiamento.aw" onPress={() => { if (typeof window !== 'undefined') window.open('https://papiamento.aw', '_blank', 'noopener,noreferrer'); }} />
+                  </View>
+                ) : null}
+
+                {professionalLanguage === 'pap_aw' && selected.status === 'completed' ? (
+                  <Pressable onPress={() => setRememberPapiamentoCorrection((value) => !value)} style={[styles.correctionCheck, rememberPapiamentoCorrection && styles.correctionCheckActive]}>
+                    <Text style={styles.correctionCheckMark}>{rememberPapiamentoCorrection ? '✓' : ''}</Text>
+                    <View style={{ flex: 1 }}><Text style={styles.correctionCheckTitle}>Guardar esta corrección para futuros reportes</Text><Text style={styles.correctionCheckCopy}>Solo se aprenderán las secciones que editaste. Déjalo sin marcar para corregir únicamente este reporte.</Text></View>
+                  </Pressable>
+                ) : null}
+
+                <View style={styles.actionRow}>
+                  {professionalLanguage === 'es' && selected.status === 'ready_for_review' ? <><Button variant="secondary" label={working ? 'Procesando…' : 'Regenerar versión breve'} disabled={working} onPress={() => void requestProfessionalReport()} /><Button variant="success" label={working ? 'Guardando…' : 'Guardar borrador'} disabled={working} onPress={() => void saveProfessionalDraft()} /></> : null}
+                  {professionalLanguage !== 'es' && selected.status === 'completed' ? <><Button variant="secondary" label={working ? 'Procesando…' : 'Regenerar traducción'} disabled={working} onPress={() => void requestProfessionalTranslation(professionalLanguage)} /><Button variant="success" label={working ? 'Guardando…' : 'Guardar traducción'} disabled={working} onPress={() => void saveProfessionalTranslation()} /></> : null}
+                  <Button variant="secondary" label={working ? 'Preparando PDF…' : `Abrir / descargar PDF · ${REPORT_LANGUAGE_COPY[professionalLanguage].code}`} disabled={working || !reportForLanguage(professionalLanguage)} onPress={() => { setWorking(true); void openCustomerReport(professionalLanguage).finally(() => setWorking(false)); }} />
+                </View>
+              </View>
+            ) : null}
+
+            {professionalLanguage === 'pap_aw' && papiamentoCorrections.length ? (
+              <View style={styles.memoryBox}>
+                <Text style={styles.memoryTitle}>Memoria aprobada de Papiamento de Aruba</Text>
+                <Text style={styles.memoryCopy}>Estas correcciones se usan como ejemplos en traducciones futuras. Puedes desactivar cualquiera sin borrarla.</Text>
+                {papiamentoCorrections.slice(0, 6).map((correction) => (
+                  <View key={correction.id} style={styles.memoryRow}>
+                    <View style={{ flex: 1 }}><Text style={styles.memorySection}>{String(correction.sectionKey)}</Text><Text style={styles.memoryText}>{correction.correctedText}</Text></View>
+                    <Button compact variant={correction.active ? 'ghost' : 'secondary'} label={correction.active ? 'Desactivar' : 'Activar'} onPress={() => void togglePapiamentoCorrection(correction)} />
+                  </View>
+                ))}
+              </View>
+            ) : null}
+          </Card>
+        ) : null}
+
+        {reportView === 'original' ? <>
 
         <Card>
           <SectionTitle title="Secciones del reporte" subtitle="Pulsa una miniatura para abrir la fotografía original sin salir del portal" />
@@ -617,7 +884,7 @@
                           ? <EvidencePreview evidence={evidence} onOpen={() => setLightboxEvidence(evidence)} />
                           : <><Text style={styles.fieldLabel}>{field.label}{field.required ? ' *' : ''}</Text><Text style={styles.missingValue}>Fotografía no disponible</Text></>
                         : field.type === 'voice_note'
-                          ? <><Text style={styles.fieldLabel}>Nota de voz · {evidence?.durationSeconds ? `${Math.floor(evidence.durationSeconds / 60)}:${String(Math.round(evidence.durationSeconds % 60)).padStart(2, '0')}` : 'duración pendiente'}</Text><Text style={styles.fieldValue}>{evidence?.transcript ?? (evidence?.transcriptionStatus === 'failed' ? 'La transcripción automática no pudo completarse; el audio original permanece guardado.' : 'Transcripción automática en proceso.')}</Text></>
+                          ? <View style={styles.voiceNoteReview}><Text style={styles.fieldLabel}>Nota de voz · {evidence?.durationSeconds ? `${Math.floor(evidence.durationSeconds / 60)}:${String(Math.round(evidence.durationSeconds % 60)).padStart(2, '0')}` : 'duración pendiente'}</Text><Text style={styles.fieldValue}>{evidence?.transcript ?? (evidence?.transcriptionStatus === 'failed' ? 'La transcripción automática no pudo completarse; el audio original permanece guardado.' : 'Transcripción automática en proceso.')}</Text>{evidence?.downloadUrl ? <Button compact variant="secondary" icon="▶" label="Escuchar nota de voz original" onPress={() => { if (typeof window !== 'undefined') window.open(evidence.downloadUrl, '_blank', 'noopener,noreferrer'); }} /> : null}</View>
                           : <><Text style={styles.fieldLabel}>{field.label}{field.required ? ' *' : ''}</Text><Text style={styles.fieldValue}>{fieldValue(value, field)}</Text></>}
                     </View>
                   );
@@ -651,6 +918,7 @@
             </View>
           </Card>
         ) : null}
+        </> : null}
 
         {selected.status === 'ready_for_review' ? (
           <Card>
@@ -760,6 +1028,17 @@
   factLabel: { color: colors.muted, fontSize: 8, fontWeight: '900', textTransform: 'uppercase' },
   factValue: { color: colors.text, fontWeight: '900', marginTop: 4 },
   internalNotice: { color: colors.muted, fontSize: 9, lineHeight: 14, marginTop: 10 },
+  reportViewTabs: { flexDirection: 'row', flexWrap: 'wrap', gap: 10 },
+  reportViewTab: { flex: 1, minWidth: 220, borderWidth: 1, borderColor: colors.border, backgroundColor: '#FFFFFF', borderRadius: 12, padding: 14 },
+  reportViewTabActive: { borderColor: colors.primary, backgroundColor: colors.primaryLight },
+  reportViewTabText: { color: colors.text, fontSize: 14, fontWeight: '900' },
+  reportViewTabTextActive: { color: colors.primaryDark },
+  reportViewTabMeta: { color: colors.muted, fontSize: 9, lineHeight: 14, marginTop: 4 },
+  languageRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 8, marginBottom: 14 },
+  languageChip: { borderWidth: 1, borderColor: colors.border, borderRadius: 999, paddingVertical: 9, paddingHorizontal: 13, backgroundColor: '#FFFFFF' },
+  languageChipActive: { borderColor: colors.primary, backgroundColor: colors.primary },
+  languageChipText: { color: colors.text, fontSize: 10, fontWeight: '900' },
+  languageChipTextActive: { color: '#FFFFFF' },
   aiProcessingBox: { backgroundColor: '#EEF6FF', borderRadius: 12, padding: 13 },
   aiProcessingTitle: { color: colors.primaryDark, fontWeight: '900' },
   aiProcessingText: { color: colors.text, fontSize: 10, lineHeight: 16, marginTop: 5 },
@@ -773,6 +1052,7 @@
   fieldRow: { borderTopWidth: 1, borderTopColor: '#EEF1F4', paddingVertical: 10 },
   fieldLabel: { color: colors.muted, fontSize: 9, fontWeight: '900', marginBottom: 5 },
   fieldValue: { color: colors.text, lineHeight: 18 },
+  voiceNoteReview: { gap: 10, alignItems: 'flex-start' },
   missingValue: { color: '#9A5A00', fontSize: 10, fontWeight: '800' },
   evidenceBox: { gap: 7, maxWidth: 420 },
   evidenceImage: { width: 300, maxWidth: '100%', height: 220, borderRadius: 12, backgroundColor: '#EEF1F5' },
@@ -785,6 +1065,17 @@
   warningBox: { backgroundColor: '#FFF8EC', borderRadius: 13, padding: 14 },
   warningTitle: { color: '#8A5200', fontWeight: '900' },
   warningText: { color: colors.text, fontSize: 10, lineHeight: 16, marginTop: 5 },
+  correctionCheck: { flexDirection: 'row', alignItems: 'flex-start', gap: 10, borderWidth: 1, borderColor: colors.border, borderRadius: 12, padding: 13, backgroundColor: '#FFFFFF' },
+  correctionCheckActive: { borderColor: colors.primary, backgroundColor: colors.primaryLight },
+  correctionCheckMark: { width: 22, height: 22, borderRadius: 5, borderWidth: 1, borderColor: colors.primary, color: colors.primary, textAlign: 'center', fontWeight: '900', lineHeight: 20 },
+  correctionCheckTitle: { color: colors.text, fontWeight: '900' },
+  correctionCheckCopy: { color: colors.muted, fontSize: 9, lineHeight: 14, marginTop: 3 },
+  memoryBox: { marginTop: 16, borderTopWidth: 1, borderTopColor: colors.border, paddingTop: 14, gap: 10 },
+  memoryTitle: { color: colors.text, fontWeight: '900', fontSize: 13 },
+  memoryCopy: { color: colors.muted, fontSize: 9, lineHeight: 14 },
+  memoryRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 10, alignItems: 'center', borderWidth: 1, borderColor: colors.border, borderRadius: 10, padding: 10 },
+  memorySection: { color: colors.primaryDark, fontSize: 8, fontWeight: '900', textTransform: 'uppercase' },
+  memoryText: { color: colors.text, fontSize: 9, lineHeight: 14, marginTop: 3 },
   successBox: { backgroundColor: '#F0F8F1', borderRadius: 13, padding: 14 },
   successTitle: { color: '#2F6A3B', fontWeight: '900' },
   successText: { color: colors.text, fontSize: 10, lineHeight: 16, marginTop: 5 },
--- a/src/services/reportPrint.ts
+++ b/src/services/reportPrint.ts
@@ -9,6 +9,8 @@
   title: string;
   status: string;
   fields: PrintableReportField[];
+  // MULTILINGUAL_PROFESSIONAL_REPORTS_V9: keep the concise narrative separate from the evidence appendix.
+  pageBreakBefore?: boolean;
 };
 
 export type PrintableTechnicalReport = {
@@ -23,6 +25,18 @@
   orderId: string;
   reportDate: string;
   observation?: string;
+  labels?: {
+    client: string;
+    property: string;
+    address: string;
+    order: string;
+    equipment: string;
+    work: string;
+    reportDate: string;
+    observation: string;
+    footer: string;
+    page: string;
+  };
   sections: PrintableReportSection[];
 };
 
@@ -69,7 +83,7 @@
   }).join('');
 
   return `
-    <section class="report-section">
+    <section class="report-section${section.pageBreakBefore ? ' page-break-before' : ''}">
       <div class="section-header">
         <h2>${escapeHtml(section.title)}</h2>
         <div class="status">${escapeHtml(section.status)}</div>
@@ -98,9 +112,10 @@
   if (!popup) return false;
 
   const sections = report.sections.map(sectionHtml).join('');
+  const labels = report.labels ?? { client: 'Cliente', property: 'Propiedad', address: 'Dirección', order: 'Orden', equipment: 'Aire acondicionado', work: 'Trabajo', reportDate: 'Fecha del reporte', observation: 'Observaciones', footer: 'Reporte técnico generado por DEMAC Professional Cooling Solutions', page: 'Página' };
   const observation = report.observation ? `
     <section class="observation">
-      <h2>OBSERVACIONES</h2>
+      <h2>${escapeHtml(labels.observation.toUpperCase())}</h2>
       <div>${escapeHtml(report.observation)}</div>
     </section>` : '';
   const downloadUrl = safeUrl(download?.url);
@@ -133,6 +148,7 @@
     .summary-label, .field-label { color: #657180; font-size: 9px; font-weight: 900; text-transform: uppercase; letter-spacing: .5px; }
     .summary-value { font-size: 12px; font-weight: 700; margin-top: 4px; overflow-wrap: anywhere; }
     .report-section { margin-top: 18px; border-top: 2px solid #0a59bd; padding-top: 10px; }
+    .page-break-before { break-before: page; page-break-before: always; }
     .section-header { display: flex; justify-content: space-between; gap: 14px; align-items: flex-start; break-after: avoid; }
     h2 { margin: 0; font-size: 16px; }
     .status { color: #24732f; background: #eaf7ec; border-radius: 999px; padding: 5px 9px; font-size: 9px; font-weight: 800; }
@@ -171,17 +187,17 @@
     <h1>${escapeHtml(report.reportTitle)}</h1>
     <div class="subtitle">${escapeHtml(report.reportCode)}</div>
     <div class="summary">
-      <div class="summary-item"><div class="summary-label">Cliente</div><div class="summary-value">${escapeHtml(report.clientName)}</div></div>
-      <div class="summary-item"><div class="summary-label">Propiedad</div><div class="summary-value">${escapeHtml(report.propertyName)}</div></div>
-      <div class="summary-item"><div class="summary-label">Dirección</div><div class="summary-value">${escapeHtml(report.address)}</div></div>
-      <div class="summary-item"><div class="summary-label">Orden</div><div class="summary-value">${escapeHtml(report.orderId)}</div></div>
-      <div class="summary-item"><div class="summary-label">Aire acondicionado</div><div class="summary-value">${escapeHtml(report.equipmentName)} · ${escapeHtml(report.equipmentDetails)}</div></div>
-      <div class="summary-item"><div class="summary-label">Trabajo</div><div class="summary-value">${escapeHtml(report.workType)}</div></div>
-      <div class="summary-item"><div class="summary-label">Fecha del reporte</div><div class="summary-value">${escapeHtml(report.reportDate)}</div></div>
+      <div class="summary-item"><div class="summary-label">${escapeHtml(labels.client)}</div><div class="summary-value">${escapeHtml(report.clientName)}</div></div>
+      <div class="summary-item"><div class="summary-label">${escapeHtml(labels.property)}</div><div class="summary-value">${escapeHtml(report.propertyName)}</div></div>
+      <div class="summary-item"><div class="summary-label">${escapeHtml(labels.address)}</div><div class="summary-value">${escapeHtml(report.address)}</div></div>
+      <div class="summary-item"><div class="summary-label">${escapeHtml(labels.order)}</div><div class="summary-value">${escapeHtml(report.orderId)}</div></div>
+      <div class="summary-item"><div class="summary-label">${escapeHtml(labels.equipment)}</div><div class="summary-value">${escapeHtml(report.equipmentName)} · ${escapeHtml(report.equipmentDetails)}</div></div>
+      <div class="summary-item"><div class="summary-label">${escapeHtml(labels.work)}</div><div class="summary-value">${escapeHtml(report.workType)}</div></div>
+      <div class="summary-item"><div class="summary-label">${escapeHtml(labels.reportDate)}</div><div class="summary-value">${escapeHtml(report.reportDate)}</div></div>
     </div>
     ${sections}
     ${observation}
-    <div class="footer">Reporte técnico generado por DEMAC Professional Cooling Solutions.</div>
+    <div class="footer">${escapeHtml(labels.footer)}.</div>
   </main>
 </body>
 </html>`);
--- a/src/services/technicalReportPdf.ts
+++ b/src/services/technicalReportPdf.ts
@@ -200,6 +200,8 @@
   const pages: PdfPage[] = [];
   let page = createPage();
   let cursor = 34;
+  // MULTILINGUAL_PROFESSIONAL_REPORTS_V9: localize customer-facing PDF chrome.
+  const labels = report.labels ?? { client: 'Cliente', property: 'Propiedad', address: 'Dirección', order: 'Orden', equipment: 'Aire acondicionado', work: 'Trabajo', reportDate: 'Fecha del reporte', observation: 'Observaciones', footer: 'Reporte técnico generado por DEMAC Professional Cooling Solutions', page: 'Página' };
 
   function addPage() {
     page = createPage();
@@ -230,13 +232,13 @@
   cursor += 20;
 
   const summary = [
-    ['Cliente', report.clientName],
-    ['Propiedad', report.propertyName],
-    ['Dirección', report.address],
-    ['Orden', report.orderId],
-    ['Aire acondicionado', `${report.equipmentName} · ${report.equipmentDetails}`],
-    ['Trabajo', report.workType],
-    ['Fecha del reporte', report.reportDate],
+    [labels.client, report.clientName],
+    [labels.property, report.propertyName],
+    [labels.address, report.address],
+    [labels.order, report.orderId],
+    [labels.equipment, `${report.equipmentName} · ${report.equipmentDetails}`],
+    [labels.work, report.workType],
+    [labels.reportDate, report.reportDate],
   ];
   const gap = 8;
   const columnWidth = (CONTENT_WIDTH - gap) / 2;
@@ -248,6 +250,7 @@
   cursor += 65;
 
   for (const section of report.sections) {
+    if (section.pageBreakBefore && cursor > 90) addPage();
     ensureSpace(54);
     line(page, MARGIN, cursor, PAGE_WIDTH - MARGIN, cursor, BLUE, 1.5);
     cursor += 10;
@@ -439,15 +442,15 @@
     const height = 42 + lines.length * 12;
     ensureSpace(height + 10);
     fillRect(page, MARGIN, cursor, CONTENT_WIDTH, height, LIGHT_BLUE, BLUE);
-    drawText(page, 'OBSERVACIONES', MARGIN + 10, cursor + 10, 9, { bold: true, fill: DARK_BLUE });
+    drawText(page, labels.observation.toUpperCase(), MARGIN + 10, cursor + 10, 9, { bold: true, fill: DARK_BLUE });
     lines.forEach((entry, index) => drawText(page, entry, MARGIN + 10, cursor + 27 + index * 12, 9, { fill: TEXT }));
     cursor += height + 10;
   }
 
   pages.forEach((item, index) => {
     line(item, MARGIN, PAGE_HEIGHT - 39, PAGE_WIDTH - MARGIN, PAGE_HEIGHT - 39, BORDER, 0.6);
-    drawText(item, 'Reporte técnico generado por DEMAC Professional Cooling Solutions', MARGIN, PAGE_HEIGHT - 31, 7, { fill: MUTED });
-    drawText(item, `Página ${index + 1} de ${pages.length}`, PAGE_WIDTH - MARGIN - 70, PAGE_HEIGHT - 31, 7, { fill: MUTED });
+    drawText(item, labels.footer, MARGIN, PAGE_HEIGHT - 31, 7, { fill: MUTED });
+    drawText(item, `${labels.page} ${index + 1} / ${pages.length}`, PAGE_WIDTH - MARGIN - 70, PAGE_HEIGHT - 31, 7, { fill: MUTED });
   });
 
   return pages;
